# Resource credit
- This is raw resources and strings extracted from MB Bank app, around early Flutter-era (v6.4.3x)

# AI?
- There is files like `singalarity_ai*` in `raw` folder, but it is not AI-generated, I replaced it with old-fashioned Google TTS voice (non-LLM one), which is likely ML (Machine-Learning) than LLM.
- The facial recognition seems to be the same too, it is in the app before Generative AI era, since 2021. It just the friendly ML (Machine-Learning).
- Even the original voice, it seems to be non-LLM, since the voice is exist in app before Generative AI era, it might have been created with TTS voice tools.
